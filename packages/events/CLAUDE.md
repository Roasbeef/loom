# events

## Scaffold

WP-K (EventBus, projections, search service) is in flight; this package
holds no hand-written source yet. Its only module today is
`events/sql.gleam` — **generated code, do not edit** — produced by the
parrot pilot (ADR-004) from `sql/schema.sql` and `src/events/sql/search.sql`
and regenerated by `scripts/gen-sql.sh`. Declared dependencies are `core`,
`storage`, `session`, `sqlight`, and `parrot` (whose `parrot/dev` the
generated module imports at runtime).

Grow this file into the full per-package shape — Purpose, Key Types,
Relationships, Traffic, Invariants — when the bus, the projections, and
the search service land. See
[docs/adr/004-parrot-sql-codegen.md](../../docs/adr/004-parrot-sql-codegen.md)
and the [root CLAUDE.md](../../CLAUDE.md).
